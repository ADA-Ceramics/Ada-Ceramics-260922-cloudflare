export { LocaleProvider, useLocale } from './context'
export { locales, defaultLocale, localeNames, localeFlagImages, type Locale } from './config'
export { t, translations } from './translations'

// Alias for useLocale for clearer naming in components
export { useLocale as useLanguage } from './context'
